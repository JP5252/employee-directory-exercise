// file for the shared interfaces between components

// interface for employee
export interface Employee {
	id: number;
	name: string;
	code: string;
	profession: string;
	color: string;
	city: string;
	branch: string;
	assigned: boolean;
  }